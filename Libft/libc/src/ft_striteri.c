#include "libft.h"
//Applies a function to each char of a string (Index specified). Returns modified prototype string
void	ft_striteri(char *s, void (*f)(unsigned int, char*))
{
	unsigned int	i;

	i = 0;
	while (s[i])
	{
		(*f)(i, &s[i]);
		i++;
	}
}
